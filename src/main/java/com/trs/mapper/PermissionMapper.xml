<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trs.mapper.PermissionMapper">
  <resultMap id="BaseResultMap" type="com.trs.model.Permission">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="parent_name" jdbcType="VARCHAR" property="parentName" />
    <result column="url" jdbcType="VARCHAR" property="url" />
    <result column="attribute" jdbcType="INTEGER" property="attribute" />
    <result column="serial" jdbcType="INTEGER" property="serial" />
  </resultMap>

  <!-- 定义resultMap role_per -->
  <resultMap id="rolePerResultMap" type="com.trs.model.RolePermission">
    <result property="roleId" column="role_id"/>
    <result property="permissionId" column="permission_id"/>
  </resultMap>
  <sql id="Permission_List">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    id, name, parent_name, url, attribute
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    <!--
      WARNING - @mbggeneratedf
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select 
    <include refid="Permission_List" />
    from permission
    where id = #{id,jdbcType=INTEGER}
  </select>

  <!-- 查询所有父标签 -->
  <!-- 去重查询菜单根目录 -->
  <select id="findAllParentName" parameterType="permission" resultType="java.lang.String">
    select parent_name from permission where attribute = #{attribute} group by parent_name,serial order by serial
  </select>
  <!-- 查询所有的功能列表 -->
  <select id="findAllName" parameterType="string" resultMap="BaseResultMap">
    select * from permission where parent_name=#{parentName} order by id
  </select>

  <!-- 添加角色与功能信息 -->
  <insert id="add" parameterType="com.trs.model.RolePermission">
    insert into role_permission(role_id,permission_id) values(#{roleId},#{permissionId})
  </insert>

  <!-- 查询角色对应的权限 -->
  <select id="listPermission" parameterType="java.lang.Integer" resultMap="rolePerResultMap">
    select permission_id from role_permission where role_id = #{id}
  </select>

  <!-- 查询角色应的 -->
  <select id="selectPermissionById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select * from permission where id = #{id}
  </select>

  <!-- 通过ID删除对应的权限 -->
  <delete id="deletePermissionById" parameterType="java.lang.Integer">
    delete FROM role_permission where role_id = #{id}
  </delete>


</mapper>